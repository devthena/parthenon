export enum ApiDataType {
  Games,
  Stats,
  Users,
}

export enum ApiDataError {
  HoneyCake = '011',
}

export enum ApiUrl {
  Games = '/api/games',
  Stats = '/api/stats',
  Users = '/api/users',
}
